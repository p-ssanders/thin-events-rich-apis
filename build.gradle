plugins {
    id 'java'
    id 'maven-publish'
    id 'org.springframework.boot' version '2.4.4' apply false
    id 'io.spring.dependency-management' version '1.0.11.RELEASE' apply false
    id 'org.springframework.cloud.contract' version '3.0.2' apply false
}

allprojects {
    group = 'dev.samsanders.demo.rabbitmq'
    version = '0.0.1-SNAPSHOT'
    sourceCompatibility = '11'

    repositories {
        mavenLocal()
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'org.springframework.boot'
    apply plugin: 'io.spring.dependency-management'

    dependencyManagement {
        imports {
            mavenBom 'org.springframework.cloud:spring-cloud-contract-dependencies:3.0.2'
        }
        dependencies {
            dependency 'org.springframework.cloud:spring-cloud-contract-gradle-plugin:3.0.2'
            dependency 'org.apache.qpid:qpid-broker-core:8.0.0'
            dependency 'org.apache.qpid:qpid-broker-plugins-amqp-0-8-protocol:8.0.0'
            dependency 'org.apache.qpid:qpid-broker-plugins-memory-store:8.0.0'
        }
    }

    test {
        useJUnitPlatform()
    }
}
